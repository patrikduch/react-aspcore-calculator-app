

react-router-dom
- klientský routovací nástroj pouze pro webové aplikace

react-detect-offline
- detekce online a offline režimu
https://www.npmjs.com/package/react-detect-offline


Root adresáře
- components (všechny možné komenty, které se využivají v rámci aplikace)
- routing (klientské routování + další konfigurace rout pro produkci i development)
- styles (stylování aplikace)


-- Pod Adresáře (components)
client obsahuje shared obsah footer, header, content
error -  komponenty pro obstarování obsluhy chyb jako 404 apod.

-- Pod adresáře (styles)

base obsahuje shared styly
components styly pro přislušné komponenty